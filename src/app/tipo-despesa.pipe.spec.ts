import { TipoDespesaPipe } from './tipo-despesa.pipe';

describe('TipoDespesaPipe', () => {
  it('create an instance', () => {
    const pipe = new TipoDespesaPipe();
    expect(pipe).toBeTruthy();
  });
});
